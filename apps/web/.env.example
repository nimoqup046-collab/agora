# AGORA Web environment variables
# For local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000

# For Docker/Render production, also set this runtime value so the web proxy
# can forward requests even when NEXT_PUBLIC_* is not injected at build time.
AGORA_API_URL=http://localhost:8000
